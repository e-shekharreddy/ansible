- name: demo om filters
  hosts: local
  connection: local
  vars:
    topics: linux, shell, ansible,terraform,docker
    Tags:
      name: ansible
      project: roboshop
      component: catalogue

  tasks:
  - name: greeting
    ansible.builtin.debug:
      msg: "Hi vardhan {{ GREETING | default('good evening') }}"

  - name: convert string to list 
    ansible.builtin.debug:
      msg: "{{ topics | split (',') }}"

  - name: convert map to list 
    ansible.builtin.debug:
      msg: "{{ tags | dict2list }}"