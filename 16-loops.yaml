- name: demo on loops
  hosts: local
  connection: local
  tasks:
  - name: prints names
    ansible.builtin.debug:
      msg: "{{ item }}"
    loop:
      - manivardhan
      - kanna
      - thadhiya