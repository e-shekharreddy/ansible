- name: demo on loops
  hosts: local
  connection: local
  tasks:
  - name: prints names
    ansible.builtin.debug:
      msg: "{{ items }}"
    loop:
      - manivardhan
      - kanna
      - thadhiya