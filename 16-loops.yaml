- name: demo on loops
  hosts: local
  connection: local
  tasks:
  - name: prints names
    ansible.builtin.debug:
      msg: "Hello, {{ item }}" # item isansible reserve keyword
    loop:
      - manivardhan
      - kanna
      - thadhiya

