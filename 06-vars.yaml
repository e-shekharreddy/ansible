- name: file variable 
  hosts: local 
  connection: local

  vars_files:
  - course.yaml

  tasks:
    - name: print variables
      vars: 
        COURCE: "MARKETING"
      ansible.builtin.debug:
       msg: "Course is {{ COURCE }}, Trainer is: {{ TAINER }}" 
